import { TDictionaryList, TLanguageOptions } from "../types";
import en from "./en.json";
import it from "./it.json";

export const dictionaryList: TDictionaryList = {
  en,
  it,
};

export const languageOptions: TLanguageOptions = {
  en: "English",
  it: "Italiano",
};
